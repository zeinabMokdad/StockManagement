@inherits System.Web.WebPages.WebPage
@RenderPage("~/Views/Shared/Shared.cshtml")

<div ng-app="mainModule" ng-controller="ProductManagementController" class="content-page">
    <div class="form-group">
        @*<table width="100%">
                <tr>
                    <td>
                        <label>
                            Name
                            <input type="text" name="Name" ng-model="productName" />
                        </label>
                    </td>
                    <td>
                        <button ng-click="onSearch()" class="btn btn-success">
                            <span class="glyphicon glyphicon-search"></span>
                            Search
                        </button>
                    </td>
                    <td>
                        <button ng-click="onAdd()" class="btn btn-success">
                            <span class="glyphicon glyphicon-plus"></span>
                            Add
                        </button>
                    </td>
                </tr>
            </table>*@
        <div class="row">
            <div class="col-lg-3">
                <label>Name</label>
                <input type="text" name="Name" ng-model="productName" />
            </div>
            <div class="col-lg-3">
                <button ng-click="onSearch()" class="btn btn-success">
                    <span class="glyphicon glyphicon-search"></span>
                    Search
                </button>
            </div>
            <div class="col-lg-3">
                <button ng-click="onAdd()" class="btn btn-success">
                    <span class="glyphicon glyphicon-plus"></span>
                    Add
                </button>
            </div>
        </div>
    </div>

    <product-grid on-ready="onProductGridReady"></product-grid>
</div>